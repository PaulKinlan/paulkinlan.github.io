function update(){const p=Number(document.querySelector('#prefill').value),n=Number(document.querySelector('#tokens').value),d=Number(document.querySelector('#decode').value);document.querySelector('#cost-shape').textContent=Number.isFinite(p+n+d)&&p>0&&n>=0&&d>0?`Illustrative only: generative ${(p+n*d).toFixed(1)}ms; decision ${(p+2).toFixed(1)}ms; ratio ${((p+n*d)/(p+2)).toFixed(1)}×. Not a measured speedup.`:'Enter positive timings and a non-negative output length.';}document.querySelectorAll('input').forEach(i=>i.addEventListener('input',update));update();
